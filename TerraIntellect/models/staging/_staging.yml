# models/staging/staging.yml

version: 2

models:
  - name: stg_clients
    columns:
      - name: client_id
        tests:
          - unique
          - not_null
  - name: stg_projects
    columns:
      - name: project_id
        tests:
          - unique
          - not_null
