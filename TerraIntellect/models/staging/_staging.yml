# models/staging/staging.yml

version: 2

models:
  - name: stg_clients
    columns:
      - name: client_id
        description: The id of the client 
        tests:
          - unique
          - not_null
  - name: stg_projects
    columns: 
      - name: project_id
        description: The id of the project
        tests:
          - unique
          - not_null
